# =============================================================================
# BHB Invoice & Dunning Portal - Umgebungsvariablen
# =============================================================================

# Datenbank
DATABASE_URL=postgres://portal:SICHERES_PASSWORT@db:5432/portal
DB_PASSWORD=SICHERES_PASSWORT

# -----------------------------------------------------------------------------
# Authentifizierung (mindestens 32 Zeichen)
# Generieren mit: openssl rand -hex 32
# -----------------------------------------------------------------------------
SESSION_SECRET=

# -----------------------------------------------------------------------------
# Verschlüsselung für API-Credentials (mindestens 32 Zeichen, anders als SESSION_SECRET)
# Generieren mit: openssl rand -hex 32
# -----------------------------------------------------------------------------
ENCRYPTION_KEY=

# -----------------------------------------------------------------------------
# Cookie-Sicherheit
# Auf "true" setzen wenn HTTPS verwendet wird (z.B. hinter Cloudflare Tunnel)
# Bei lokalem HTTP-Zugriff auf "false" lassen oder weglassen
# -----------------------------------------------------------------------------
COOKIE_SECURE=false

# -----------------------------------------------------------------------------
# System-Update Secret (erforderlich für Self-Update-Funktion)
# Generieren mit: openssl rand -hex 32
# Muss gesetzt sein, um das automatische Update zu aktivieren
#
# SICHERHEITSHINWEISE:
# - Port 5001 ist der interne Admin-Port - NIEMALS extern veröffentlichen!
# - Port 5001 verwendet 'expose:' (nicht 'ports:') in docker-compose.yml
# - Nur vertrauenswürdige Container im portal-network erreichen Port 5001
# - Das UPDATE_SECRET verhindert unbefugte Update-Auslösungen
# -----------------------------------------------------------------------------
UPDATE_SECRET=

# -----------------------------------------------------------------------------
# Cloudflare Tunnel (optional, wird direkt beim docker run übergeben)
# Siehe DEPLOYMENT.md für den Befehl
# -----------------------------------------------------------------------------
